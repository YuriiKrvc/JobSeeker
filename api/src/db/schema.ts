/**
 * Drizzle table definitions — the source of truth for the database.
 *
 * Empty on purpose. The `postings` table is not defined yet because the
 * duplicate-handling rule it has to enforce is still undecided (see the Open
 * decisions section of CLAUDE.md). Defining it now would bake in an answer.
 *
 * After adding a table here, run `npm run db:generate` and commit the SQL.
 */
export {}
